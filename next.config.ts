import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // cssModules: {
  //   localsConvention: "dashes", // 类名格式：xxx-xxx
  //   generateScopedName: "[name]__[local]___[hash:base64:5]", // 类名规则
  // },
};

export default nextConfig;
